# Public site URL (used for canonical + OG)
VITE_SITE_URL=https://veyra-medical-aesthetics.vercel.app

# Optional: point frontend at a secure AI endpoint (never put secrets in VITE_*)
# VITE_AI_API_URL=/api/ai-guide
# VITE_AI_PROVIDER=mock

# Optional booking endpoint
# VITE_BOOKING_API_URL=/api/book

# Server-only (Vercel Environment Variables) — NEVER prefix with VITE_
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=
# AI_PROVIDER=openai
